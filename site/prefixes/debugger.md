# 前缀：debugger

前缀 `debugger` 下的全部路由，主要归入“调试器”相关页面或入口。

- 路由数：`10`

## `bilibili://debugger/account/sms`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / account / sms”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/account/sms" />

## `bilibili://debugger/bh/main`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / bh / 主站”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/bh/main" />

## `bilibili://debugger/setting/api`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / api”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/api" />

## `bilibili://debugger/setting/brpc`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / brpc”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/brpc" />

## `bilibili://debugger/setting/http-scheme-check`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / http scheme check”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/http-scheme-check" />

## `bilibili://debugger/setting/mod`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / mod”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/mod" />

## `bilibili://debugger/setting/neuron`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / neuron”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/neuron" />

## `bilibili://debugger/setting/oaid`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / oaid”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/oaid" />

## `bilibili://debugger/setting/tribe-bundle`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / tribe bundle”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/tribe-bundle" />

## `bilibili://debugger/setting/web_env`

::: info 路由说明
- 类型：`native`
- 说明：bilibili 协议 路由，归入“调试器 / 设置 / web env”，类型为 原生。
- 关联入口：无公开同组别名
:::

### 参数说明

- 无显式路径参数。

### 快速操作

<RouteActions route="bilibili://debugger/setting/web_env" />
